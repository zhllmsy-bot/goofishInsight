# Schema Package

## Purpose

`packages/schema` holds shared schema notes and cross-runtime payload references.

## Boundary

Use this package for shared shape documentation, not for Python ORM source of truth. The live database models remain in `apps/collector/src/goofish_insight/models.py`.
